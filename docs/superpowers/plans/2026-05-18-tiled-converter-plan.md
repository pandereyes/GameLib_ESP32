# Tiled 地图编辑器集成 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Tiled `.tmx`/`.tsx` → C 代码的完整转换管线，支持图层数据、tile 属性自动发现、对象层导出

**Architecture:** Python 转换器 (`tiled2c.py`) 解析 Tiled XML，自动发现所有属性名并按类型生成位掩码/数组，输出 `map_data.h` + `map_data.c`。运行时通过生成的宏（零开销）或新增的 3 个 gamelib API 函数查询 tile 属性。

**Tech Stack:** Python 3 (xml.etree), C11 (ESP-IDF)

---

### 文件结构

```
tools/tiled2c.py              ← 重写：通用转换器
components/gamelib_core/
  gamelib.h                    ← 修改：新增 tileprop 类型 + 查询 API 声明 + map_obj_t
  gamelib.c                    ← 修改：新增 3 个 tile 属性查询函数实现
main/
  CMakeLists.txt               ← 修改：添加 map_data.c 到 SRCS
  demos/demo_17_rpg_demo_resource/tiled_project/
    map_data.h                 ← 生成：转换器输出（替换旧版）
    map_data.c                 ← 生成：转换器输出（替换旧版）
```

---

### Task 1: 在 gamelib.h 中添加 tile 属性类型和 API 声明

**Files:**
- Modify: `components/gamelib_core/gamelib.h`

- [ ] **Step 1: 在 gamelib.h 的 tilemap 相关声明前插入类型定义和 API 声明**

在 `/* --- tilemap --- */` 注释块之前（约第 153 行前）插入以下代码：

```c
/* --- tile property types (generated by tiled2c.py) --- */
typedef enum {
    TILEPROP_TYPE_BOOL = 0,
    TILEPROP_TYPE_INT  = 1,
    TILEPROP_TYPE_FLOAT = 2,
} tileprop_type_t;

typedef struct {
    int              firstgid;
    const char      *name;
    tileprop_type_t  type;
    const void      *data;
    int              count;       /* elements (bool: word count) */
} tileprop_desc_t;

/* --- tile property query (runtime) --- */
bool  gamelib_tile_prop_bool (int firstgid, const char *name, int tile_gid);
int   gamelib_tile_prop_int  (int firstgid, const char *name, int tile_gid);
float gamelib_tile_prop_float(int firstgid, const char *name, int tile_gid);

/* --- object type (generated by tiled2c.py) --- */
typedef struct {
    int   id;
    float x, y;
    float w, h;
    char  name[32];
} map_obj_t;
```

- [ ] **Step 2: 验证修改**

确认 gamelib.h 语法无误（结构体/枚举定义在 `#ifdef __cplusplus` 块内）：

```
grep -n "tileprop_type_t\|tileprop_desc_t\|map_obj_t" components/gamelib_core/gamelib.h
```

预期：看到新增的类型定义行号，都在 `extern "C" {` 之后。

---

### Task 2: 在 gamelib.c 中实现 tile 属性查询函数

**Files:**
- Modify: `components/gamelib_core/gamelib.c`

- [ ] **Step 1: 在 gamelib.c 开头 extern 区域添加查找表声明**

在 `font_t *g_fonts = NULL;` 之后（第 10 行后）添加：

```c
extern const tileprop_desc_t tileprop_table[];
extern const int             tileprop_table_size;
```

- [ ] **Step 2: 在 gamelib.c 末尾添加 3 个查询函数实现**

在文件末尾（`gamelib_file_close` 之后，`#ifdef __cplusplus` 之前）添加：

```c
/* --- tile property query --- */
bool gamelib_tile_prop_bool(int firstgid, const char *name, int tile_gid)
{
    int local_id = tile_gid - firstgid;
    for (int i = 0; i < tileprop_table_size; i++) {
        const tileprop_desc_t *d = &tileprop_table[i];
        if (d->firstgid == firstgid && d->type == TILEPROP_TYPE_BOOL &&
            strcmp(d->name, name) == 0) {
            if (local_id < 0 || local_id >= d->count * 32) return false;
            const uint32_t *bits = (const uint32_t*)d->data;
            return (bits[local_id >> 5] >> (local_id & 0x1F)) & 1;
        }
    }
    return false;
}

int gamelib_tile_prop_int(int firstgid, const char *name, int tile_gid)
{
    int local_id = tile_gid - firstgid;
    for (int i = 0; i < tileprop_table_size; i++) {
        const tileprop_desc_t *d = &tileprop_table[i];
        if (d->firstgid == firstgid && d->type == TILEPROP_TYPE_INT &&
            strcmp(d->name, name) == 0) {
            if (local_id < 0 || local_id >= d->count) return 0;
            const int32_t *arr = (const int32_t*)d->data;
            return (int)arr[local_id];
        }
    }
    return 0;
}

float gamelib_tile_prop_float(int firstgid, const char *name, int tile_gid)
{
    int local_id = tile_gid - firstgid;
    for (int i = 0; i < tileprop_table_size; i++) {
        const tileprop_desc_t *d = &tileprop_table[i];
        if (d->firstgid == firstgid && d->type == TILEPROP_TYPE_FLOAT &&
            strcmp(d->name, name) == 0) {
            if (local_id < 0 || local_id >= d->count) return 0.0f;
            const float *arr = (const float*)d->data;
            return arr[local_id];
        }
    }
    return 0.0f;
}
```

- [ ] **Step 3: 验证编译**

```bash
cd e:/1.work/4.HUAYING/1.project/11.GameLib_MCU/1.source/gamelib_for_esp32
idf.py build
```

预期：链接错误 `undefined reference to tileprop_table` 和 `tileprop_table_size` — 这是正常的，因为还没有生成 map_data.c。只要 gamelib.c 本身编译通过即可。

---

### Task 3: 重写 tiled2c.py 转换器

**Files:**
- Create: `tools/tiled2c.py`

- [ ] **Step 1: 编写完整的转换器代码**

写入 `tools/tiled2c.py`：

```python
#!/usr/bin/env python3
"""
Tiled .tmx → C 代码转换器
用法: python tiled2c.py <map.tmx> <map_data.c> <map_data.h>

自动发现 .tsx 中所有 tile 自定义属性, 按类型生成:
- bool   → uint32_t 位掩码 + TILE_HAS_XXX(tile_idx) 宏
- int    → int32_t 数组   + TILE_GET_XXX(tile_idx) 宏
- float  → float 数组     + TILE_GET_XXX(tile_idx) 宏

同时导出图层数据 + 对象层数据。
"""
import xml.etree.ElementTree as ET
import sys
import os
import re
import math

def safe_c_name(s):
    """将字符串转换为合法的 C 标识符"""
    s = re.sub(r'[^a-zA-Z0-9_]', '_', s)
    if s and s[0].isdigit():
        s = '_' + s
    return s.lower() if s else 'unnamed'

def parse_properties(elem):
    """解析 Tiled properties 元素, 返回 {name: (type, value)} 字典"""
    props = {}
    props_elem = elem.find('properties')
    if props_elem is None:
        return props
    for p in props_elem.findall('property'):
        name = p.attrib.get('name', '')
        ptype = p.attrib.get('type', 'string')
        value = p.attrib.get('value', '')
        if ptype == 'bool':
            props[name] = ('bool', value.lower() == 'true')
        elif ptype == 'int':
            props[name] = ('int', int(value) if value else 0)
        elif ptype == 'float':
            props[name] = ('float', float(value) if value else 0.0)
        else:
            # 跳过不支持的类型 (string/color/file/object)
            pass
    return props

def discover_tile_properties(root):
    """扫描所有 tile, 收集属性名→类型映射"""
    prop_types = {}  # name → type (bool/int/float)
    for tile in root.findall('tile'):
        props = parse_properties(tile)
        for name, (ptype, _) in props.items():
            if name not in prop_types:
                prop_types[name] = ptype
            elif prop_types[name] != ptype:
                print(f"Warning: property '{name}' has conflicting types, using first seen")
    return prop_types

def build_tile_property_arrays(root, prop_types, tilecount):
    """为每种属性构建完整数组 (tilecount 个元素)"""
    # 初始化默认值数组
    bool_arrays = {}   # name → list[bool]
    int_arrays = {}    # name → list[int]
    float_arrays = {}  # name → list[float]

    for name, ptype in prop_types.items():
        if ptype == 'bool':
            bool_arrays[name] = [False] * tilecount
        elif ptype == 'int':
            int_arrays[name] = [0] * tilecount
        elif ptype == 'float':
            float_arrays[name] = [0.0] * tilecount

    # 填充有属性值的 tile
    for tile in root.findall('tile'):
        tile_id = int(tile.attrib.get('id', 0))
        if tile_id < 0 or tile_id >= tilecount:
            continue
        props = parse_properties(tile)
        for name, (ptype, value) in props.items():
            if ptype == 'bool' and name in bool_arrays:
                bool_arrays[name][tile_id] = value
            elif ptype == 'int' and name in int_arrays:
                int_arrays[name][tile_id] = value
            elif ptype == 'float' and name in float_arrays:
                float_arrays[name][tile_id] = value

    return bool_arrays, int_arrays, float_arrays

def write_bool_bitmask(fc, name, values):
    """输出 bool 位掩码数组"""
    word_count = (len(values) + 31) // 32
    words = [0] * word_count
    for i, v in enumerate(values):
        if v:
            words[i >> 5] |= (1 << (i & 0x1F))

    fc.write(f'const uint32_t tileprop_{name}[{word_count}] = {{\n')
    for i in range(0, len(words), 8):
        chunk = words[i:i+8]
        fc.write('    ' + ', '.join(f'0x{w:08X}U' for w in chunk) + ',\n')
    fc.write('};\n\n')

def write_int_array(fc, name, values):
    """输出 int32_t 数组"""
    fc.write(f'const int32_t tileprop_{name}[{len(values)}] = {{\n')
    for i in range(0, len(values), 16):
        chunk = values[i:i+16]
        fc.write('    ' + ', '.join(str(v) for v in chunk) + ',\n')
    fc.write('};\n\n')

def write_float_array(fc, name, values):
    """输出 float 数组"""
    fc.write(f'const float tileprop_{name}[{len(values)}] = {{\n')
    for i in range(0, len(values), 8):
        chunk = values[i:i+8]
        parts = []
        for v in chunk:
            if v == int(v) and abs(v) < 1e6:
                parts.append(f'{v:.1f}f')
            else:
                parts.append(f'{v!r}f')
        fc.write('    ' + ', '.join(parts) + ',\n')
    fc.write('};\n\n')

def parse_layer_data(layer_elem):
    """提取图层 CSV 数据, 返回 [int, ...]"""
    data_elem = layer_elem.find('data')
    if data_elem is None:
        return None
    encoding = data_elem.attrib.get('encoding', 'csv')
    if encoding != 'csv':
        return None
    text = data_elem.text or ''
    text = text.strip().replace('\n', '').replace('\r', '').replace(' ', '')
    result = []
    for s in text.split(','):
        s = s.strip()
        result.append(int(s) if s else 0)
    return result

def parse_objects(root):
    """解析所有 objectgroup, 按 type 分组返回 {type: [obj_dict, ...]}"""
    groups = {}
    for og in root.findall('objectgroup'):
        for obj in og.findall('object'):
            oid = int(obj.attrib.get('id', 0))
            otype = obj.attrib.get('type', '')
            oname = obj.attrib.get('name', '')
            ox = float(obj.attrib.get('x', 0))
            oy = float(obj.attrib.get('y', 0))
            ow = float(obj.attrib.get('width', 0))
            oh = float(obj.attrib.get('height', 0))

            if not otype:
                continue  # 没有 type 的对象跳过

            props = parse_properties(obj)

            if otype not in groups:
                groups[otype] = []
            groups[otype].append({
                'id': oid,
                'name': oname[:31] if oname else '',
                'x': ox, 'y': oy,
                'w': ow, 'h': oh,
                'props': props,
            })
    return groups

def write_object_data(fc, groups):
    """输出对象数据"""
    for otype, objs in groups.items():
        ctype = safe_c_name(otype)
        count = len(objs)

        # struct 数组
        fc.write(f'// === object group: "{otype}" ===\n')
        fc.write(f'const map_obj_t map_obj_{ctype}[{count}] = {{\n')
        for obj in objs:
            fc.write(f'    {{{obj["id"]}, {obj["x"]:.1f}f, {obj["y"]:.1f}f, '
                     f'{obj["w"]:.1f}f, {obj["h"]:.1f}f, "{obj["name"]}"}},\n')
        fc.write('};\n\n')

        # 收集对象属性名
        obj_prop_types = {}
        for obj in objs:
            for name, (ptype, _) in obj['props'].items():
                if name not in obj_prop_types:
                    obj_prop_types[name] = ptype

        # 为每种对象属性生成数组
        for pname, ptype in obj_prop_types.items():
            cpname = safe_c_name(pname)
            if ptype == 'bool':
                word_count = (count + 31) // 32
                words = [0] * word_count
                for i, obj in enumerate(objs):
                    if pname in obj['props'] and obj['props'][pname][1]:
                        words[i >> 5] |= (1 << (i & 0x1F))
                fc.write(f'const uint32_t obj_{ctype}_{cpname}[{word_count}] = {{\n    ')
                fc.write(', '.join(f'0x{w:08X}U' for w in words))
                fc.write('\n};\n\n')
            elif ptype == 'int':
                vals = [obj['props'].get(pname, ('int', 0))[1] for obj in objs]
                fc.write(f'const int32_t obj_{ctype}_{cpname}[{count}] = {{\n    ')
                fc.write(', '.join(str(v) for v in vals))
                fc.write('\n};\n\n')
            elif ptype == 'float':
                vals = [obj['props'].get(pname, ('float', 0.0))[1] for obj in objs]
                fc.write(f'const float obj_{ctype}_{cpname}[{count}] = {{\n    ')
                fc.write(', '.join(f'{v:.1f}f' if v == int(v) else f'{v!r}f' for v in vals))
                fc.write('\n};\n\n')

def write_header(fh, basename, map_w, map_h, tile_w, tile_h, tilesets,
                 layers, bool_arrays, int_arrays, float_arrays, obj_groups):
    """生成 map_data.h"""
    guard = f'{basename.upper()}_H'

    fh.write('/* Auto-generated by tiled2c.py — DO NOT EDIT */\n')
    fh.write(f'#ifndef {guard}\n')
    fh.write(f'#define {guard}\n\n')
    fh.write('#include <stdint.h>\n')
    fh.write('#include <stdbool.h>\n')
    fh.write('#include "gamelib.h"\n\n')

    fh.write('/* === Map metadata === */\n')
    fh.write(f'#define MAP_WIDTH        {map_w}\n')
    fh.write(f'#define MAP_HEIGHT       {map_h}\n')
    fh.write(f'#define MAP_TILE_WIDTH   {tile_w}\n')
    fh.write(f'#define MAP_TILE_HEIGHT  {tile_h}\n\n')

    for ts in tilesets:
        fh.write(f'/* tileset "{ts["name"]}" */\n')
        fh.write(f'#define MAP_TILESET_{safe_c_name(ts["name"]).upper()}_FIRSTGID  {ts["firstgid"]}\n')
        fh.write(f'#define MAP_TILESET_{safe_c_name(ts["name"]).upper()}_TILECOUNT  {ts["tilecount"]}\n')
        fh.write(f'#define MAP_TILESET_{safe_c_name(ts["name"]).upper()}_COLUMNS    {ts["columns"]}\n\n')

    fh.write('/* === Layer data === */\n')
    for lyr in layers:
        fh.write(f'extern const int16_t map_layer_{lyr["cname"]}[MAP_WIDTH * MAP_HEIGHT];\n')
    fh.write('\n')

    if bool_arrays or int_arrays or float_arrays:
        fh.write('/* === Tile property bitmasks/arrays === */\n')

        for name in sorted(bool_arrays.keys()):
            word_count = (tilesets[0]["tilecount"] + 31) // 32 if tilesets else 0
            uname = name.upper()
            fh.write(f'extern const uint32_t tileprop_{safe_c_name(name)}[{word_count}];\n')
            fh.write(f'#define TILEPROP_{uname}_EXISTS  1\n')
            fh.write(f'#define TILEPROP_{uname}_WORDS   {word_count}\n')
            fh.write(f'#define TILE_HAS_{uname}(tile_idx)  '
                     f'((tileprop_{safe_c_name(name)}[(tile_idx) >> 5] >> ((tile_idx) & 0x1F)) & 1)\n\n')

        for name in sorted(int_arrays.keys()):
            tc = len(int_arrays[name])
            uname = name.upper()
            fh.write(f'extern const int32_t tileprop_{safe_c_name(name)}[{tc}];\n')
            fh.write(f'#define TILEPROP_{uname}_EXISTS  1\n')
            fh.write(f'#define TILE_GET_{uname}(tile_idx)  (tileprop_{safe_c_name(name)}[tile_idx])\n\n')

        for name in sorted(float_arrays.keys()):
            tc = len(float_arrays[name])
            uname = name.upper()
            fh.write(f'extern const float tileprop_{safe_c_name(name)}[{tc}];\n')
            fh.write(f'#define TILEPROP_{uname}_EXISTS  1\n')
            fh.write(f'#define TILE_GET_{uname}(tile_idx)  (tileprop_{safe_c_name(name)}[tile_idx])\n\n')

    if obj_groups:
        fh.write('/* === Object layer data === */\n')
        for otype, objs in obj_groups.items():
            ctype = safe_c_name(otype)
            utype = otype.upper()
            fh.write(f'extern const map_obj_t map_obj_{ctype}[{len(objs)}];\n')
            fh.write(f'#define MAP_OBJ_{utype}_COUNT  {len(objs)}\n\n')

    fh.write(f'#endif /* {guard} */\n')

def write_source(fc, h_basename, map_w, map_h, tilesets, layers,
                 bool_arrays, int_arrays, float_arrays, obj_groups,
                 prop_firstgids):
    """生成 map_data.c"""
    fc.write('/* Auto-generated by tiled2c.py — DO NOT EDIT */\n')
    fc.write(f'#include "{h_basename}"\n')
    fc.write('#include <string.h>\n\n')

    if layers:
        fc.write('/* ================================================================ */\n')
        fc.write('/*  Layer data                                                     */\n')
        fc.write('/* ================================================================ */\n\n')

    for lyr in layers:
        fc.write(f'const int16_t map_layer_{lyr["cname"]}[{map_w * map_h}] = {{\n')
        for r in range(0, len(lyr["data"]), map_w):
            row = lyr["data"][r:r + map_w]
            fc.write('    ' + ', '.join(str(v) for v in row) + ',\n')
        fc.write('};\n\n')

    if bool_arrays or int_arrays or float_arrays:
        fc.write('/* ================================================================ */\n')
        fc.write('/*  Tile properties                                                */\n')
        fc.write('/* ================================================================ */\n\n')

        for name in sorted(bool_arrays.keys()):
            write_bool_bitmask(fc, safe_c_name(name), bool_arrays[name])

        for name in sorted(int_arrays.keys()):
            write_int_array(fc, safe_c_name(name), int_arrays[name])

        for name in sorted(float_arrays.keys()):
            write_float_array(fc, safe_c_name(name), float_arrays[name])

        # 生成查找表 (每个属性的 firstgid 来自对应 tileset)
        fc.write('/* === Property lookup table === */\n')
        entries = []
        for name in sorted(bool_arrays.keys()):
            wc = (len(bool_arrays[name]) + 31) // 32
            fgid = prop_firstgids.get(name, 1)
            entries.append(f'    {{{fgid}, "{name}", TILEPROP_TYPE_BOOL, '
                           f'tileprop_{safe_c_name(name)}, {wc}}}')
        for name in sorted(int_arrays.keys()):
            tc = len(int_arrays[name])
            fgid = prop_firstgids.get(name, 1)
            entries.append(f'    {{{fgid}, "{name}", TILEPROP_TYPE_INT, '
                           f'tileprop_{safe_c_name(name)}, {tc}}}')
        for name in sorted(float_arrays.keys()):
            tc = len(float_arrays[name])
            fgid = prop_firstgids.get(name, 1)
            entries.append(f'    {{{fgid}, "{name}", TILEPROP_TYPE_FLOAT, '
                           f'tileprop_{safe_c_name(name)}, {tc}}}')

        fc.write(f'const tileprop_desc_t tileprop_table[{len(entries)}] = {{\n')
        fc.write(',\n'.join(entries))
        fc.write('\n};\n\n')
        fc.write(f'const int tileprop_table_size = {len(entries)};\n\n')

    if obj_groups:
        fc.write('/* ================================================================ */\n')
        fc.write('/*  Object layer data                                              */\n')
        fc.write('/* ================================================================ */\n\n')
        write_object_data(fc, obj_groups)

def parse_tileset(tsx_path, firstgid):
    """解析 .tsx 文件, 返回 tileset 信息字典"""
    tree = ET.parse(tsx_path)
    root = tree.getroot()

    ts_name = root.attrib.get('name', 'unnamed')
    tilecount = int(root.attrib.get('tilecount', 0))
    columns = int(root.attrib.get('columns', 0))
    tilewidth = int(root.attrib.get('tilewidth', 0))
    tileheight = int(root.attrib.get('tileheight', 0))

    # 图集图片信息
    img = root.find('image')
    img_source = img.attrib.get('source', '') if img is not None else ''
    img_width = int(img.attrib.get('width', 0)) if img is not None else 0
    img_height = int(img.attrib.get('height', 0)) if img is not None else 0

    # 发现所有属性
    prop_types = discover_tile_properties(root)
    bool_arrs, int_arrs, float_arrs = build_tile_property_arrays(root, prop_types, tilecount)

    return {
        'name': ts_name,
        'firstgid': firstgid,
        'tilecount': tilecount,
        'columns': columns,
        'tilewidth': tilewidth,
        'tileheight': tileheight,
        'img_source': img_source,
        'img_width': img_width,
        'img_height': img_height,
        'bool_arrays': bool_arrs,
        'int_arrays': int_arrs,
        'float_arrays': float_arrs,
    }

def main():
    if len(sys.argv) < 4:
        print("Usage: python tiled2c.py <map.tmx> <map_data.c> <map_data.h>")
        sys.exit(1)

    tmx_path = sys.argv[1]
    out_c = sys.argv[2]
    out_h = sys.argv[3]

    tmx_dir = os.path.dirname(os.path.abspath(tmx_path))

    # 解析 .tmx
    tree = ET.parse(tmx_path)
    root = tree.getroot()

    map_w = int(root.attrib.get('width', 0))
    map_h = int(root.attrib.get('height', 0))
    tile_w = int(root.attrib.get('tilewidth', 0))
    tile_h = int(root.attrib.get('tileheight', 0))

    # 解析 tileset 引用
    tilesets = []
    for ts_elem in root.findall('tileset'):
        firstgid = int(ts_elem.attrib.get('firstgid', 1))
        source = ts_elem.attrib.get('source', '')
        if source:
            tsx_path = os.path.join(tmx_dir, source)
            if not os.path.exists(tsx_path):
                print(f"Error: tileset file not found: {tsx_path}")
                sys.exit(1)
            ts_info = parse_tileset(tsx_path, firstgid)
        else:
            # 内嵌 tileset (不常见, 简化处理)
            print(f"Warning: embedded tileset not fully supported, skipping properties")
            ts_info = {
                'name': ts_elem.attrib.get('name', 'unnamed'),
                'firstgid': firstgid,
                'tilecount': int(ts_elem.attrib.get('tilecount', 0)),
                'columns': int(ts_elem.attrib.get('columns', 0)),
                'tilewidth': int(ts_elem.attrib.get('tilewidth', 0)),
                'tileheight': int(ts_elem.attrib.get('tileheight', 0)),
                'img_source': '',
                'img_width': 0, 'img_height': 0,
                'bool_arrays': {}, 'int_arrays': {}, 'float_arrays': {},
            }
        tilesets.append(ts_info)

    # 合并所有 tileset 的属性数组 (目前每个 tileset 独立生成)
    # 对于多图集, 生成前缀区分, 同时记录每个属性的 firstgid
    all_bool = {}
    all_int = {}
    all_float = {}
    prop_firstgids = {}  # name → firstgid
    for ts in tilesets:
        prefix = f'{safe_c_name(ts["name"])}_' if len(tilesets) > 1 else ''
        for name, arr in ts['bool_arrays'].items():
            merged_name = prefix + name
            all_bool[merged_name] = arr
            prop_firstgids[merged_name] = ts['firstgid']
        for name, arr in ts['int_arrays'].items():
            merged_name = prefix + name
            all_int[merged_name] = arr
            prop_firstgids[merged_name] = ts['firstgid']
        for name, arr in ts['float_arrays'].items():
            merged_name = prefix + name
            all_float[merged_name] = arr
            prop_firstgids[merged_name] = ts['firstgid']

    # 解析图层
    layers = []
    for layer_elem in root.findall('layer'):
        name = layer_elem.attrib.get('name', 'unnamed')
        data = parse_layer_data(layer_elem)
        if data is None:
            print(f"Warning: layer '{name}' skipped (non-CSV encoding)")
            continue
        if len(data) != map_w * map_h:
            print(f"Warning: layer '{name}' data size mismatch "
                  f"({len(data)} != {map_w * map_h}), padding/truncating")
            while len(data) < map_w * map_h:
                data.append(0)
            data = data[:map_w * map_h]
        layers.append({'name': name, 'cname': safe_c_name(name), 'data': data})

    # 解析对象层
    obj_groups = parse_objects(root)

    # 生成 .h
    h_basename = os.path.basename(out_h)
    with open(out_h, 'w', encoding='utf-8') as fh:
        write_header(fh, h_basename.replace('.', '_'), map_w, map_h, tile_w, tile_h,
                     tilesets, layers, all_bool, all_int, all_float, obj_groups)

    # 生成 .c
    c_basename = os.path.basename(out_h)
    with open(out_c, 'w', encoding='utf-8') as fc:
        write_source(fc, c_basename, map_w, map_h, tilesets, layers,
                     all_bool, all_int, all_float, obj_groups, prop_firstgids)

    print(f"Generated: {out_h}, {out_c}")
    print(f"  Map: {map_w}x{map_h}, tile: {tile_w}x{tile_h}")
    print(f"  Layers: {len(layers)}")
    print(f"  Tilesets: {len(tilesets)}")
    if all_bool:
        print(f"  Bool properties: {', '.join(sorted(all_bool.keys()))}")
    if all_int:
        print(f"  Int properties: {', '.join(sorted(all_int.keys()))}")
    if all_float:
        print(f"  Float properties: {', '.join(sorted(all_float.keys()))}")
    if obj_groups:
        print(f"  Object groups: {', '.join(f'{k}({len(v)})' for k, v in obj_groups.items())}")

if __name__ == '__main__':
    main()
```

- [ ] **Step 2: 在现有 Tiled 项目上运行转换器**

```bash
cd e:/1.work/4.HUAYING/1.project/11.GameLib_MCU/1.source/gamelib_for_esp32
python tools/tiled2c.py \
  "main/demos/demo_17_rpg_demo_resource/tiled_project/map.tmx" \
  "main/demos/demo_17_rpg_demo_resource/tiled_project/map_data.c" \
  "main/demos/demo_17_rpg_demo_resource/tiled_project/map_data.h"
```

预期输出：
```
Generated: .../map_data.h, .../map_data.c
  Map: 30x20, tile: 16x16
  Layers: 2
  Tilesets: 1
  Bool properties: obstacle
  Int properties: (如果有)
  Float properties: (如果有)
```

- [ ] **Step 3: 检查生成的 map_data.h 内容**

验证生成的头文件包含：
- `MAP_WIDTH 30`, `MAP_HEIGHT 20`
- `MAP_TILE_WIDTH 16`, `MAP_TILE_HEIGHT 16`
- `map_layer_layer_0_base[600]` 和 `map_layer_layer_1[600]` extern 声明
- `tileprop_obstacle[19]` extern 声明
- `TILEPROP_OBSTACLE_EXISTS` 宏
- `TILE_HAS_OBSTACLE(tile_idx)` 宏
- `tileprop_desc_t tileprop_table[]` extern 声明
- `map_obj_t` 类型（在 gamelib.h 中）

- [ ] **Step 4: 检查生成的 map_data.c 内容**

验证生成的源文件包含：
- `#include "map_data.h"`
- 图层数据数组
- obstacle 位掩码 (19 个 uint32_t)
- 查找表数组
- 对象组数据（如有）

---

### Task 4: 更新 CMakeLists.txt 以包含生成的 map_data.c

**Files:**
- Modify: `main/CMakeLists.txt`

- [ ] **Step 1: 将 map_data.c 添加到编译源文件**

将 `main/CMakeLists.txt` 第 1 行：
```cmake
idf_component_register(SRCS "main.c"
```
改为：
```cmake
idf_component_register(SRCS "main.c"
                    "demos/demo_17_rpg_demo_resource/tiled_project/map_data.c"
```

完整的 CMakeLists.txt 变为：
```cmake
idf_component_register(SRCS "main.c"
                    "demos/demo_17_rpg_demo_resource/tiled_project/map_data.c"
                    INCLUDE_DIRS "."
                    REQUIRES gamelib_core gamelib_port_esp32s3)
```

- [ ] **Step 2: 验证编译**

```bash
cd e:/1.work/4.HUAYING/1.project/11.GameLib_MCU/1.source/gamelib_for_esp32
idf.py build
```

预期：编译通过，链接成功（tileprop_table 和 tileprop_table_size 在 map_data.c 中定义）。

---

### Task 5: 更新 demo_17_rpg_demo.c 使用新 API

**Files:**
- Modify: `main/demos/demo_17_rpg_demo.c`

- [ ] **Step 1: 在 demo 中添加 tilemap 加载和障碍物检测示例**

在 `demo_17_rpg_demo.c` 的 `#include "gamelib.h"` 之后添加 map_data 头文件引用，并在初始化部分添加 tilemap 的加载和渲染逻辑。

具体修改（在文件中添加 tilemap 演示功能）：

在主循环之前（约第 38 行 action 加载代码块之后），添加 tilemap 初始化：

```c
    /* --- Tilemap 加载演示 --- */
    #include "demo_17_rpg_demo_resource/tiled_project/map_data.h"

    /* 加载 tileset 图片 */
    int tileset_id = -1;
    {
        size_t len;
        uint8_t *buf = load_file(g, "demo_17_rpg_demo_resource/tilemap.png", &len);
        if (buf) {
            tileset_id = gamelib_sprite_load_png(g, buf, len);
            free(buf);
        }
    }

    /* 创建 tilemap 并填充数据 */
    int map_id = gamelib_tilemap_create(g, MAP_WIDTH, MAP_HEIGHT,
                                         MAP_TILE_WIDTH, tileset_id);
    if (map_id >= 0) {
        for (int r = 0; r < MAP_HEIGHT; r++) {
            for (int c = 0; c < MAP_WIDTH; c++) {
                int idx = r * MAP_WIDTH + c;
                /* 底层 (layer_0_base) 作为基础地形 */
                gamelib_tilemap_set_tile(g, map_id, c, r,
                                         map_layer_layer_0_base[idx]);
            }
        }
    }

    /* 玩家位置 */
    int player_x = 120, player_y = 160;
    int player_speed = 2;
```

- [ ] **Step 2: 在主循环中添加 tilemap 渲染和障碍物碰撞**

修改主循环（约第 77 行之后）以包含 tilemap 绘制和移动碰撞：

```c
    while (!gamelib_is_closed(g)) {
        if (gamelib_is_key_pressed(g, KEY_SELECT)) break;

        /* 玩家移动 + 障碍物检测 */
        int new_x = player_x, new_y = player_y;
        if (gamelib_is_key_down(g, KEY_UP))    new_y -= player_speed;
        if (gamelib_is_key_down(g, KEY_DOWN))  new_y += player_speed;
        if (gamelib_is_key_down(g, KEY_LEFT))  new_x -= player_speed;
        if (gamelib_is_key_down(g, KEY_RIGHT)) new_x += player_speed;

        /* 边界和障碍物检测 */
        if (new_x >= 0 && new_x < MAP_WIDTH * MAP_TILE_WIDTH &&
            new_y >= 0 && new_y < MAP_HEIGHT * MAP_TILE_HEIGHT) {
            int tile_gid = gamelib_tilemap_get_at_pixel(g, map_id, new_x + 8, new_y + 8);
            if (tile_gid > 0) {
                int local_id = tile_gid - MAP_TILESET_TILEMAP_FIRSTGID;
                if (!TILE_HAS_OBSTACLE(local_id)) {
                    player_x = new_x;
                    player_y = new_y;
                }
            }
        }

        /* 渲染 */
        gamelib_clear(g, COLOR_BLACK);

        /* 绘制 tilemap (底层) */
        gamelib_tilemap_draw(g, map_id, 0, 0);

        /* 绘制上层装饰 (layer_1) */
        for (int r = 0; r < MAP_HEIGHT; r++) {
            for (int c = 0; c < MAP_WIDTH; c++) {
                int tid = map_layer_layer_1[r * MAP_WIDTH + c];
                if (tid <= 0) continue;
                int local_id = tid - MAP_TILESET_TILEMAP_FIRSTGID;
                int sx = (local_id % MAP_TILESET_TILEMAP_COLUMNS) * MAP_TILE_WIDTH;
                int sy = (local_id / MAP_TILESET_TILEMAP_COLUMNS) * MAP_TILE_HEIGHT;
                gamelib_draw_sprite_region(g, tileset_id, c * MAP_TILE_WIDTH, r * MAP_TILE_HEIGHT,
                                           sx, sy, MAP_TILE_WIDTH, MAP_TILE_HEIGHT);
            }
        }

        /* 绘制玩家 */
        int spr_id = sprites[cur_action];
        int sx = player_x - DST_FRAME_W / 2;
        int sy = player_y - DST_FRAME_H / 2;
        gamelib_draw_sprite_frame_scaled(g, spr_id, sx, sy,
                                          FRAME_W, FRAME_H, cur_frame,
                                          DST_FRAME_W, DST_FRAME_H,
                                          SPRITE_ALPHA);

        /* 绘制对象层 entity (如有) */
        #ifdef MAP_OBJ_NPC_COUNT
        for (int i = 0; i < MAP_OBJ_NPC_COUNT; i++) {
            gamelib_fill_rect(g, (int)map_obj_npc[i].x, (int)map_obj_npc[i].y,
                              16, 16, COLOR_YELLOW);
        }
        #endif

        /* HUD */
        gamelib_draw_printf(g, 5, 5, COLOR_WHITE,
                            "Pos: %d,%d", player_x, player_y);
        gamelib_draw_printf(g, 5, 18, COLOR_GRAY,
                            "Action:%d/%d", cur_action, action_count - 1);
        gamelib_draw_printf(g, 5, H - 12, COLOR_DARKGRAY,
                            "DPAD:move  L/R:action  SEL:quit");

        gamelib_update(g);
        gamelib_wait_frame(g);
    }

    /* 清理 tilemap */
    if (map_id >= 0) {
        gamelib_tilemap_free(g, map_id);
    }
```

- [ ] **Step 3: 验证编译**

```bash
cd e:/1.work/4.HUAYING/1.project/11.GameLib_MCU/1.source/gamelib_for_esp32
idf.py build
```

预期：编译通过，无错误。

---

### Task 6: 端到端验证

- [ ] **Step 1: 完整构建验证**

```bash
idf.py build
```

预期：BUILD SUCCESS，无 warning 和 error。

- [ ] **Step 2: 验证生成的宏可用性**

在 demo 代码或单独检查中确认以下宏可正常使用：
- `TILE_HAS_OBSTACLE(local_id)` — 检查障碍物
- `MAP_OBJ_xxx_COUNT` — 对象数量
- `map_obj_xxx[i].x/y` — 对象坐标

- [ ] **Step 3: 验证属性灵活性**

在 Tiled 中为 tile 新增一个 `int` 类型属性（如 `damage`），保存后运行转换器：

```bash
python tools/tiled2c.py \
  "main/demos/demo_17_rpg_demo_resource/tiled_project/map.tmx" \
  "main/demos/demo_17_rpg_demo_resource/tiled_project/map_data.c" \
  "main/demos/demo_17_rpg_demo_resource/tiled_project/map_data.h"
```

预期：`map_data.h` 自动包含 `TILEPROP_DAMAGE_EXISTS` 和 `TILE_GET_DAMAGE(tile_idx)` 宏，无需修改转换器。
